# NEO Player Alpha 0.5

This branch is an isolated Alpha 0.5 snapshot based on the current feature line rooted at commit `7717f88f64e88008cc31f25b99dbbcee32fa70ea`.

- `main` is not modified by this branch.
- Existing application features are retained.
- Alpha 0.5 build metadata is `versionCode = 5` and `versionName = "0.5.0-alpha"`.
- This marker exists so the Alpha 0.5 CI verifies it is building the prepared isolated branch rather than an incomplete bootstrap state.
