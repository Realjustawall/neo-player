package com.neoplayer.app.ui

/**
 * Classifier alias kept separate from the Compose Column callable so additive UI helpers can use
 * a concise receiver type without rewriting or removing any existing Neo+ surface.
 */
typealias Column = androidx.compose.foundation.layout.ColumnScope
